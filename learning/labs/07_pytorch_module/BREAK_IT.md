# Break It

Introduce one unit, shape, ordering, leakage or stability defect relevant to **PyTorch module**. Write the expected failure before running. Then add an assertion or test that catches the defect at the earliest boundary.
