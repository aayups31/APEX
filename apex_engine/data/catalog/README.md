# Data Catalog

Create one manifest per downloaded session under a local, ignored directory. A manifest should include source, query, access time, source version, row counts, hashes, schema version, terms URL and whether redistribution is permitted.

Do not store authentication tokens, paid API responses or bulk historical timing/telemetry in Git.
